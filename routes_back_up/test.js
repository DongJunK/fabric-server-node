const express = require('express');
const router = express.Router();

router.get('/users/:id',(req, res, next) =>{
	
});

router.post('/users/:from/:to/:money/',(req, res, next) =>{
	res.send('ex) a b 20');
	

	/*
		Enter a code to send a request to the blockchain network.
	*/
});

module.exports = router;
